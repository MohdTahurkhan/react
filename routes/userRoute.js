const express = require("express");
const router = express.Router();

// //apiuser +/
// router.get("/",function(req,res,next){
//     res.status(200).json({ sucess: true, message: "Welcome to the User API" });
// });

router.post("/create", );

  


module.exports = router;